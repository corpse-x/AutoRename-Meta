user:
  name: corpse-x
  github: ghost

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "ffmpeg"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"